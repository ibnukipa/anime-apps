import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    paddingTop: 16,
    alignItems: 'center',
  },
  captionContainer: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  captionIcon: {
    marginRight: 8,
  },
});

export default styles;
